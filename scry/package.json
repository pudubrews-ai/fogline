{
  "name": "@fogline/scry",
  "version": "0.9.0",
  "description": "Scry: read-only 3D observatory for a Fogline world. Renders what the daemon publishes; decides nothing. Live from the operator channel or replay from a saved ticks.log \u2014 one reducer for both.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "analyst": "node analyst/server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
