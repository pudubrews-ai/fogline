{
  "name": "@fogline/client",
  "version": "0.9.0",
  "description": "Fogline client: supplies decisions for one agent. The only place in the system where a model credential exists.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1"
  }
}
